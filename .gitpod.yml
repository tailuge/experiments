tasks:
 - init: >
        sudo apt update &&
        sudo apt install zstd shellcheck &&
        pip install torch -index-url https://download.pytorch.org/whl/cpu &&
        pip install onnx &&
        echo "done"
